import org.junit.Assert;
import org.junit.Test;

public class LocationManagerTest {
    @Test
    public void 현재_위치_정보_출력(){
        LocationManager locationManager = new LocationManager();
        locationManager.getGio();

        Assert.assertTrue(false);
    }
}
